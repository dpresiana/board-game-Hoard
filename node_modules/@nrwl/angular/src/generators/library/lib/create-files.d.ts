import type { ProjectConfiguration, Tree } from '@nrwl/devkit';
import { NormalizedSchema } from './normalized-schema';
export declare function createFiles(tree: Tree, options: NormalizedSchema, project: ProjectConfiguration & {
    prefix: string;
}): void;
